현재 Vue 3 + Vite 프로젝트 `Ssafy_We_Are`에 실제 동작하는 LocalHub를 구현해줘. 설명만 하지 말고 필요한 Vue 파일, 라우터, 유틸리티를 생성·수정해줘.

절대 조건:

- Vue 3 Composition API와 `<script setup>`만 사용한다.
- 새 npm 패키지는 설치하지 않는다.
- 백엔드, DB, 로그인, 회원가입은 만들지 않는다.
- API 키를 코드에 하드코딩하거나 출력하지 않는다.
- `.env` 파일은 수정하거나 Git에 포함하지 않는다.
- 아래 환경 변수명을 정확히 사용한다.

```env
VITE_KAKAO_MAP_API_KEY
VITE_KAKAO_REST_API_KEY
VITE_TMAP_APP_KEY
VITE_OPENAI_API_KEY
```

- 모든 화면 텍스트는 한국어로 작성한다.
- 구현 뒤 `npm run build`가 통과하도록 검토한다.

제공 데이터는 `public/data/`에 있다.

- `cctv_gwangju.clean.json`
  - `{ id, lat, lng, count, sourceIds, type }`
  - CCTV 994건을 중복 좌표 기준으로 통합한 445개 마커 데이터
- `police_gwangju.clean.json`
  - `{ id, name, district, type, address, mapReady }`
  - 경찰 시설 40건, 좌표가 없으므로 지도 마커로 직접 표시하지 않는다.
- `gwangju-data-summary.json`
  - CCTV와 경찰 시설 통계

라우터:

- `/` 홈
- `/map` 안전지도
- `/community` 게시글 목록
- `/community/write` 글 작성
- `/community/:id` 글 상세
- `/community/:id/edit` 글 수정

`App.vue`에는 상단 네비게이션을 구현한다.

- 서비스명: LocalHub
- 메뉴: 홈, 안전지도, 커뮤니티
- 모바일 대응

홈 화면:

- “광주 안전 정보 커뮤니티” 소개
- 안전지도·커뮤니티 이동 버튼
- localStorage 최신 게시글 3개 표시
- CCTV 총 대수와 경찰 시설 수 표시
- 모든 페이지 우측 하단에 챗봇 플로팅 버튼 표시

안전지도 `Map.vue`:

1. npm 패키지 없이 카카오 지도 JavaScript SDK를 동적으로 한 번만 로드한다.

```js
https://dapi.kakao.com/v2/maps/sdk.js?appkey=${import.meta.env.VITE_KAKAO_MAP_API_KEY}&autoload=false&libraries=clusterer,services
```

2. `window.kakao` 접근 전 `kakao.maps.load` 완료를 보장한다.
3. 초기 지도는 광주 중심 좌표 `35.1595, 126.8526`, level 8이다.
4. `cctv_gwangju.clean.json`을 fetch로 불러와 카카오 `MarkerClusterer`로 CCTV 마커 445개를 표시한다.
5. 마커 클릭 시 “이 위치 CCTV n대”, 위도, 경도를 InfoWindow로 표시한다.
6. CCTV 표시/숨김 체크박스를 만든다.
7. CCTV 실제 대수(count 합계), CCTV 마커 수, 경찰 시설 수 카드 3개를 표시한다.
8. 현재 위치 버튼은 `navigator.geolocation`을 사용한다. 성공 시 출발지 마커와 지도 이동, 실패 시 오류 문구를 표시한다.
9. 지도 클릭 시 목적지 마커를 생성·이동한다.
10. 주소 검색은 카카오 `Geocoder.addressSearch`를 사용한다.
11. 경찰 시설은 구(district) 검색 가능한 목록으로 표시하고, 시설 클릭 시 주소를 목적지로 검색한다.
12. 길찾기 버튼은 출발지와 목적지가 있을 때 활성화한다. 현재 위치가 없으면 광주 중심을 출발지로 사용한다.
13. TMAP 보행자 길찾기 API를 호출한다.

```text
POST https://apis.openapi.sk.com/tmap/routes/pedestrian?version=1&format=json
```

```js
headers: {
  appKey: import.meta.env.VITE_TMAP_APP_KEY,
  'Content-Type': 'application/json'
}
```

요청 body에는 `startX`, `startY`, `endX`, `endY`, `startName`, `endName`을 사용한다. X는 경도, Y는 위도다.

14. 응답 GeoJSON의 `LineString` 좌표 `[lng, lat]`를 `kakao.maps.LatLng(lat, lng)`로 변환해 `kakao.maps.Polyline` 경로를 지도에 표시한다.
15. 새 길찾기 전에 기존 경로를 제거한다.
16. `totalDistance`, `totalTime`으로 거리와 예상 시간을 표시한다.
17. API 키 누락, SDK 로드 실패, 길찾기 실패, CORS 오류, 빈 데이터 상태를 화면에 안내한다.
18. 모바일 지도 높이는 최소 420px, 데스크톱은 최소 560px이다.

익명 커뮤니티 CRUD:

- `src/utils/communityStorage.js`를 만든다.
- localStorage 키: `localhub-posts-v1`
- 게시글 형식:

```js
{
  id: crypto.randomUUID(),
  title: string,
  content: string,
  category: '안전제보' | '동네정보' | '질문' | '자유',
  password: string,
  createdAt: ISO 문자열,
  updatedAt: ISO 문자열,
  viewCount: number
}
```

- localStorage 파싱 오류 시 빈 배열로 복구한다.
- 서버가 없으므로 글은 현재 브라우저에만 저장된다는 안내를 표시한다.
- `CommunityList.vue`
  - 카테고리 탭, 제목·내용 검색, 최신순 목록, 글쓰기 버튼, 페이지당 10개, 페이지네이션
- `PostEditor.vue`
  - 작성·수정 공용
  - 제목 2~80자, 내용 10~2000자, 비밀번호 4자 이상 검증
- `PostDetail.vue`
  - 첫 진입 시 조회수 +1
  - 수정·삭제 버튼
  - 수정·삭제 시 비밀번호 모달
  - 성공 시 수정 화면 이동 또는 삭제 후 목록 이동

챗봇 `SafetyChatbot.vue`:

- 모든 페이지 우측 하단에 플로팅 버튼을 표시한다.
- 클릭 시 채팅 패널을 열고 닫는다.
- 모바일에서는 화면 대부분 크기로 표시한다.
- 메시지 목록, 입력창, 전송 버튼, 전송 중 상태, 오류 상태, 대화 지우기 버튼을 구현한다.
- Enter 전송, Shift+Enter 줄바꿈.
- 채팅 기록은 localStorage 키 `localhub-chat-v1`에 저장하고 최근 20개만 보존한다.
- `VITE_OPENAI_API_KEY`가 없으면 API 호출 대신 설정 안내를 표시한다.
- fetch로 OpenAI Chat Completions API를 직접 호출한다.

```text
POST https://api.openai.com/v1/chat/completions
```

```js
headers: {
  Authorization: `Bearer ${import.meta.env.VITE_OPENAI_API_KEY}`,
  'Content-Type': 'application/json'
}
```

```js
{
  model: 'gpt-5-mini',
  messages,
  temperature: 0.4,
  max_completion_tokens: 500
}
```

- `choices[0].message.content`를 답변으로 표시한다.
- API 키와 Authorization 헤더는 console.log로 출력하지 않는다.
- 401, 429, 네트워크 오류, 빈 응답을 구분해 한국어 오류 문구를 보여준다.
- 챗봇에는 CCTV 전체 좌표를 전송하지 않는다.
- 컨텍스트에는 CCTV·경찰 통계, 경찰 시설 40개 이름·구·유형·주소, 최신 게시글 최대 10개만 넣는다.
- 시스템 메시지 규칙:
  - 제공 데이터 밖의 사실을 만들어내지 않는다.
  - 긴급 상황은 112 또는 119 신고를 먼저 안내한다.
  - 답변은 간결한 한국어로 작성한다.

필요한 파일 예시:

- `src/components/SafetyChatbot.vue`
- `src/utils/communityStorage.js`
- `src/views/CommunityList.vue`
- `src/views/PostDetail.vue`
- `src/views/PostEditor.vue`
- `src/views/Map.vue`
- `src/views/Home.vue`
- `src/router/index.js`
- `src/App.vue`

각 화면에 로딩·빈 상태·오류 상태를 구현하고, scoped CSS로 반응형 화면을 완성해줘. 구현 완료 후 변경 파일 목록과 `.env` 변수명만 짧게 알려줘.